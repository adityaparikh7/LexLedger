/**
 * emailService.ts
 *
 * NOTE: SMTP sending has been removed from LexLedger.
 * All email delivery is handled via native mail clients (Apple Mail, Outlook)
 * or browser-based compose URLs (Gmail, Outlook Web, Yahoo Mail).
 *
 * This file is retained for future reference and can be extended if SMTP
 * support is re-introduced.
 */

export {};